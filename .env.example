# Environment Configuration
# Copy this file to .env and fill in your actual values

# JWT Secret Key - IMPORTANTE: Altere esta chave em produção!
# Gere uma chave segura com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# ATENÇÃO: O valor abaixo é apenas um placeholder e será rejeitado pela aplicação
JWT_SECRET=GENERATE_A_SECURE_JWT_SECRET_WITH_THE_COMMAND_ABOVE

# Porta do servidor
PORT=3000

# Ambiente de execução
NODE_ENV=development

# Database Configuration (if needed in future)
# DATABASE_URL=your-database-connection-string

# Node Environment
NODE_ENV=development